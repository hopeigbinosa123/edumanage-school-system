import React from "react";

function StudentRecords({ students, setStudents }) {
  return (
    <div>
      <h2>Student Records</h2>
      {/* Later: add form, table, CRUD operations */}
    </div>
  );
}

export default StudentRecords;